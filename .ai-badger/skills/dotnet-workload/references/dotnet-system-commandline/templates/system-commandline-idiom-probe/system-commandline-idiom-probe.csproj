<Project Sdk="Microsoft.NET.Sdk">
  <!-- Throwaway probe: pin System.CommandLine behavior for whatever version restores.
       Copy this folder into /tmp (never into the repo under review). Adjust the
       PackageReference to the version you need to pin. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="System.CommandLine" Version="2.0.10" />
  </ItemGroup>
</Project>
